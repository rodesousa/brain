# Index

> **Ce fichier est la carte du brain pour le harness / l'agent.** À lire EN ENTIER en début de
> session. Chaque ligne = un sujet récupérable. Pour répondre à « le truc sur X », faire le fuzzy
> match entre la demande et le nom + les mots-clés de la ligne, puis ouvrir
> `wiki/<sujet>/<sujet>.md` (le digest). Si l'index ne matche pas, réécrire la requête 2-3 façons
> (synonymes, autre angle) avant de conclure « introuvable ».

Maintenance : une ligne par sujet. Sujet déprécié = marqué `(deprecated)`, le fichier n'est jamais
supprimé. Les fichiers dans `wiki/<sujet>/sources/` ne sont jamais listés ici.

## Sujets

- [[munder_difflin]] — Munder Difflin : un harness multi-agents desktop qui enveloppe les CLIs d'agents existants (Claude Code, Codex, Grok…) en une équipe coordonnée par un orchestrateur GOD, mémoire markdown-first (hive), circuit breaker, étage d'avatars Pixi. _mots-clés : harness, multi-agent, orchestrateur, hive, mémoire-markdown, circuit-breaker, claude-code_
- [[freetoken]] — FreeToken (FlashML) : moteur de serving MoE edge-native, modèles frontière 284B-753B sur PC perso via CPU-GPU co-execution bandwidth-adaptive (offload/hybrid), LRU expert cache, caching sémantique, APIs OpenAI/Anthropic. _mots-clés : freetoken, flashml, moe, inference, serving, offload, hybrid, deepseek-v4, qwen3.6, glm-5.2, llm-local_
